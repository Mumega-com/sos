{
  "additionalProperties": false,
  "description": "Usage event for budget tracking (one call / one session slice).",
  "properties": {
    "agent_id": {
      "title": "Agent Id",
      "type": "string"
    },
    "cost": {
      "description": "Cost in cents.",
      "minimum": 0,
      "title": "Cost",
      "type": "integer"
    },
    "tenant_id": {
      "title": "Tenant Id",
      "type": "string"
    },
    "tokens": {
      "description": "Combined input+output tokens.",
      "minimum": 0,
      "title": "Tokens",
      "type": "integer"
    }
  },
  "required": [
    "tenant_id",
    "agent_id",
    "tokens",
    "cost"
  ],
  "title": "RecordAgentUsageRequest",
  "type": "object"
}
