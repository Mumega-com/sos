"""Kernel policy module — capability models + FMAAP policy engine skeleton.

Moved here from ``sos/services/common/`` in v0.4.4 Wave 4. The logic is a stub
inherited verbatim from the services layer; v0.5.0 will replace it with the
real policy engine. Imports should target ``sos.kernel.policy.capability``,
``sos.kernel.policy.fmaap``, and ``sos.kernel.policy.capability_auth``.
"""
