# {{DISPLAY_NAME}} — environment variables
# Copy to `.env` (gitignored) and fill in secrets.

# Bus token — minted at provision time; check tokens.json
SOS_BUS_TOKEN=

# Mirror key — DB-issued when configured; cached DB token IDs are revalidated before reuse
MIRROR_KEY=

# Tenant scope identifiers
TENANT_SLUG={{TENANT_SLUG}}
TENANT_DISPLAY_NAME={{DISPLAY_NAME}}
TENANT_INDUSTRY={{INDUSTRY}}
