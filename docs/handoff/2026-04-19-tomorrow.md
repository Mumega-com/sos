# Handoff — sos-dev, morning of 2026-04-20

**Written by:** sos-dev (end of 2026-04-19 session)
**For:** sos-dev, next session — and Hadi, who said "we are going to use sos in work from tomorrow"
**Read first:** this file top to bottom, then `docs/plans/2026-04-19-mumega-mothership.md` Phase 4 section.

## Headline

**api.mumega.com is now the mumega-edge unified router.** Phase 4 cutover
landed today. The CF catchall (`*.mumega.com/* → inkwell-api`) was
swallowing the hostname even after a green `wrangler deploy` — the fix
was a specific route (`api.mumega.com/* → mumega-edge`, route id
`c7bcd11cf5b342588d7f037c26776b74`). Rollback = delete that route; the
catchall takes over again.

Reference memory saved:
`~/.claude/projects/-mnt-HC-Volume-104325311-SOS/memory/reference_cf_worker_routes.md`
— read this before any future edge deploy.

## What shipped today

**SOS main (0878a773 → d5b1ee95):**

| Commit | What |
|---|---|
| `a3ed496f` | feat: squad done_when gate — T1.3 Part 2 (task #270). Migration 0002 + NotAllDoneError + round-trip tests. |
| `d5b1ee95` | docs: phase-4 completion sprint + adapter extension plan + edge architecture note. |

**mumega-site main (5ad062e → 6823760):**

| Commit | What |
|---|---|
| `6823760` | content: "The Junction Disappears" blog post. Kay Hermes voice — three tests for invisible infrastructure. |

**mumega-edge:** version `9df75722-3c83-430f-a65d-f35dbd24be1b`,
commit `f14faac`. Route `api.mumega.com/*` pinned to `mumega-edge` script.

## GitHub issue state

**Closed today:**
- SOS planning issues: #99–#109, #122, #123 (13 issues, stale v0.5 planning)
- mumega-edge#1 (Phase 4 coordination)
- inkwell#8, inkwell#25 (stale v4 items)
- inkwell#30 via SOS #276 (worker role resolved by Phase 4 cutover)

**Open and load-bearing:**
- SOS: #77, #78, #81, #82, #128 (product-readiness inventory)
- inkwell: 8 issues — Phase 4 coordination + potentially-active v4 items
- mumega-edge: 0

**Task-tracker pending (SOS):**
- #205 EPIC Mothership
- #209 Phase 4 (in_progress — SOS side ✅; Inkwell per-instance cutover still pending)
- #213 Phase 8 — onboard the 8 projects (v1.0.0)
- #266 T1.2 — task.completed → WorkSettlement.settle
- #267 T1.3 — structured done_when on Objective (Part 1; part 2 = #270 done)
- #272 INFRA-001 cross-repo coordination (in_progress)
- #273 Fix gen_ts_types.sh to emit PortInterface types (inkwell#29)
- #274 Verify sos init Step B template interpolation (inkwell#27)
- #275 Scope Notion + GCS/S3/CRM adapter slice (inkwell#26 + inkwell#23)

## Critical path to first-tenant operation (from #128)

The product-readiness inventory filed today lays out HAVE / LACK / blockers.
If we're operating tomorrow, the ordered gates are:

1. **CORS allowlist expansion in mumega-edge/src/index.ts:18** — currently
   only `https://mumega.com` and `https://api.mumega.com`. Before any
   per-instance site (`digid.com`, `shabrang.ai`, `*.pages.dev`)
   cuts over, this has to include their origins. Blocks inkwell#31.
2. **Rotate CF API token** `WFBrSTwCbLGlij2CuWDh909bBY1-3MECW2UxY8_K` —
   shared in plaintext this session. Token has full deploy+routes scope.
3. **Wire task.completed → WorkSettlement.settle (#266)** — done_when
   gate is live but economic settlement on completion isn't yet.
4. **Adapter extension point (docs/plans/2026-04-19-sos-adapter-extension-point.md)**
   — Option B recommended, not yet sized as a task. Blocks any
   Jira/Linear/ClickUp integration from landing cleanly.

## Running services

No service restarts today. The Phase 4 work lives on CF Workers, not the
Python services.

| Port | Service | State |
|---|---|---|
| :6070 | sos_mcp_sse | unchanged |
| :8060 | squad service | unchanged (migration 0002 applied at next restart) |
| :8075 | saas registry | unchanged |
| :8090 | dashboard | unchanged |
| :8844 | mirror-api | unchanged |

**Action for morning:** restart squad service to pick up migration 0002
(`done_when_json` column) and the `/complete` gate. Existing rows default
to `[]` so legacy tasks still complete vacuously.

## Local working-tree state (SOS)

Clean except for `sos/bus/tokens.json` (runtime e2e-test token noise — a
`.backup` exists already; intentionally not committed). `.env` contains
the CF token and is gitignored (verified).

## Notes for the next session

- Hadi asked if SOS should have a system to support adapters. Plan doc
  written (`docs/plans/2026-04-19-sos-adapter-extension-point.md`) —
  Option B (elevate BaseConnector + entry-point discovery) recommended.
  Not yet scheduled. Blocks any Linear/ClickUp/Jira ask with a real
  answer.
- The blog post on mumega-site ("The Junction Disappears") is the
  canonical public framing for Mumega = product / SOS = engine /
  Inkwell = skin. Reuse its three tests for "invisible" when discussing
  infra quality.
- MEMORY.md has a new reference entry for CF Worker routes. Read it
  before `wrangler deploy` against any production hostname.
