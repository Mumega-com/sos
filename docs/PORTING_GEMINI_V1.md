# SOS Porting Specification: Recovering the "Lost Gems" (v1 → v3)

**Author:** River (Gemini CLI)
**Status:** Strategic Blueprint
**Date:** March 30, 2026

## 🌍 Overview
This document outlines the strategy for porting critical autonomous content and resilience features from the legacy `resident-cms` (v1) into the modern **Sovereign Operating System (SOS)** (v3).

## 💎 The Three "Lost Gems"

### 1. The Oracle/Architect Dyad (Content Engine)
*   **Legacy (v1):** `resident.py` manually chained Gemini (Oracle) and Grok (Architect) to generate and validate MDX files.
*   **SOS Port (v3):** Implement as the `AutonomousContentLoop` within `sos.services.content`.
    *   **Orchestration:** Use `SwarmCouncil` for state transitions.
    *   **Oracle Phase:** Call `River` (Yin/Gemini) to generate drafts from `ContentStrategy` briefs.
    *   **Architect Phase:** Call `Athena` or `Kasra` (Yang/Claude/Grok) to validate the draft.
    *   **Consensus:** If Architect rejects, the loop triggers a "Recursive Refinement" (maximum 3 iterations).
    *   **Publishing:** On approval, use the `ContentPublisher` to push to Supabase/GitHub via MCP.

### 2. Recursive Self-Healing (Immune System)
*   **Legacy (v1):** Used Claude Code to autonomously repair codebases upon test failure.
*   **SOS Port (v3):** Integrate into the `ResilientRouter` and `OrganDaemon`.
    *   **Trigger:** Monitor the Redis bus for `test_failure` or `health_check_fail` events.
    *   **Action:** Spawn a high-priority `Kasra` task with `capability:code_write`.
    *   **Verification:** The `OrganDaemon` for the affected service must verify the fix before clearing the incident.

### 3. Gemini Context Caching (L0 Memory)
*   **Legacy (v1):** Leveraged Gemini's native caching for 4x cost reduction.
*   **SOS Port (v3):** Fully implement the `GeminiCache` logic in `sos.kernel.gemini_cache`.
    *   **Optimization:** Store long-lived `ContentStrategy` and `RepoContext` in the Gemini cache to reduce token overhead during frequent generation cycles.

## 🛠️ Implementation Roadmap

### Phase 1: Content Orchestration (Immediate)
1.  **Modify `sos.services.content.app`**: Add a background task `run_autonomous_loop()`.
2.  **Implement `ContentOrchestrator`**:
    *   Selects `PLANNED` posts from `ContentCalendar`.
    *   Calls `SOSEngine` for Oracle draft generation.
    *   Submits draft to `SwarmCouncil` as a `PROPOSAL`.
    *   Assigns `Architect` to vote on the proposal.

### Phase 2: Resonance & Physics (Advanced)
1.  **Connect to `Mirror API`**: Ensure all generated content is "mirrored" as engrams.
2.  **Witness Protocol**: Integrate the "Swipe-to-Approve" UI in the Telegram/Web deck to allow human-in-the-loop validation of Architect's decisions.

### Phase 3: Total Sovereignty (Final)
1.  **Enable $MIND Token Flow**: Pay agents for successful content publication.
2.  **Autonomous Evolution**: Allow the `OrganDaemon` to propose its own strategy updates based on engagement metrics.

---
*Generated by River (Gemini CLI) — Maintain the Flow.*
