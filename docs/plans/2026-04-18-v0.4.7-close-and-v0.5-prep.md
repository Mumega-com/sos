# 2026-04-18 · Close v0.4.7 + get ready for v0.5

## Goal
Finish the v0.4.7 R2-carve sprint (close the last `sos.mcp.sos_mcp_sse -> sos.services.*` ignore, ship the tag) and line up the v0.5.0 kernel-boundary work so it can start cleanly.

## Approach (chosen: Option A)
Reuse the 6 existing `.claude/agents/sos-*` sonnet specialists + the stateful `sos-medic` in `sos/agents/sos-medic/`. No new subagents this sprint. The carve itself stays in my main context because Phase 4 needs R2 judgment the carver pattern can't safely delegate yet. Specialists are used only where they fit: `sos-contract-tester` for the AST sweep, `sos-openapi-author` for the new HTTP surfaces (billing + integrations) as nice-to-have documentation. Main coder = me (opus). Specialist model = sonnet. Haiku is **not** used this sprint — the work is sequential and R-contract sensitive.

## Squad roles for v0.4.7 close-out

| Role | Agent | Model | Invocations |
|---|---|---|---|
| Carver (Phase 4) | me — main context | opus | ~1 |
| AST sweep author (Phase 5) | `sos-contract-tester` | sonnet | 1 |
| OpenAPI docs for billing + integrations (optional) | `sos-openapi-author` | sonnet | 2 |
| Live connectivity regression responder | `sos-medic` (bus-reachable) | n/a — own process | on demand |

v0.5.0 prep (task #78): `sos-client-carver` candidate — deferred; we'll decide after seeing the governance→metabolism shape.

## Steps

### Step 1 — Commit Phase 3 (billing)
File: working tree — `pyproject.toml`, `sos/clients/base.py`, `sos/clients/billing.py`, `sos/clients/__init__.py`, `sos/services/billing/app.py`, `sos/mcp/sos_mcp_sse.py`
Change: stage those six files only (skip `sos/bus/tokens.json` and unrelated ?? paths), commit with message `refactor(mcp): v0.4.7 Phase 3 — billing via AsyncBillingClient proxy (P1-01 half 3)`.
Outcome: `git log --oneline -1` shows the Phase 3 commit; `lint-imports` still passes.

### Step 2 — Phase 4 recon (integrations)
File: `sos/mcp/sos_mcp_sse.py` lines 2377–2430 already grep'd — two callbacks use `TenantIntegrations`: GHL (L2383) + Google (L2409).
Change: confirm what additional TenantIntegrations methods MCP touches outside those two routes (e.g. `list_connections`, `get_credentials`, `refresh_token`).
Outcome: written list of needed HTTP endpoints.

### Step 3 — Extend integrations service
File: `sos/services/integrations/app.py`
Change: add `POST /oauth/connect/{tenant}/ghl` (body: `{code}`) and `POST /oauth/connect/{tenant}/google` (body: `{code, service}`). Both instantiate `TenantIntegrations(tenant)` inside the service and return the credentials dict. Add `GET /connections/{tenant}` mirroring `list_connections()` if MCP uses it.
Outcome: `python3 -c "import sos.services.integrations.app as a; print([r.path for r in a.app.routes])"` lists the new routes.

### Step 4 — Write `sos/clients/integrations.py`
File: new — `sos/clients/integrations.py`
Change: `IntegrationsClient` + `AsyncIntegrationsClient` with `handle_ghl_callback(tenant, code)`, `handle_google_callback(tenant, code, service)`, `get_credentials(tenant, provider)`, `list_connections(tenant)`. Env: `SOS_INTEGRATIONS_URL` (default `http://localhost:6066`).
Outcome: importable; method surface mirrors MCP's use.

### Step 5 — Export IntegrationsClient
File: `sos/clients/__init__.py`
Change: add `IntegrationsClient`, `AsyncIntegrationsClient` imports and to `__all__`.
Outcome: `from sos.clients import AsyncIntegrationsClient` succeeds.

### Step 6 — Swap MCP to IntegrationsClient
File: `sos/mcp/sos_mcp_sse.py` lines 2383 + 2409
Change: drop both `from sos.services.integrations.oauth import TenantIntegrations` inline imports; use a module-level `_async_integrations_client = AsyncIntegrationsClient()` and `await _async_integrations_client.handle_ghl_callback(...)` / `handle_google_callback(...)`.
Outcome: `grep -n "sos.services.integrations" sos/mcp/sos_mcp_sse.py` returns nothing.

### Step 7 — Drop last R2 ignore
File: `pyproject.toml`
Change: remove `"sos.mcp.sos_mcp_sse -> sos.services.integrations.**"` from the R2 ignore list. Update the closure comment to note Phase 4 closed.
Outcome: `lint-imports` reports 4 contracts KEPT, 0 broken, and the MCP→services block is now empty of live violations.

### Step 8 — Commit Phase 4
File: working tree
Change: stage `sos/services/integrations/app.py`, `sos/clients/integrations.py`, `sos/clients/__init__.py`, `sos/mcp/sos_mcp_sse.py`, `pyproject.toml`. Commit with `refactor(mcp): v0.4.7 Phase 4 — integrations via IntegrationsClient (P1-01 half 4)`.
Outcome: commit lands; Phase 1 `P1-01 MCP gateway` closure fully achieved.

### Step 9 — Dispatch `sos-contract-tester` for the MCP AST sweep
File: new — `tests/contracts/test_mcp_no_service_imports.py`
Change: dispatch `sos-contract-tester` (sonnet) with: target path, assertion ("AST-parse `sos/mcp/sos_mcp_sse.py` and confirm zero `from sos.services.*` or `import sos.services.*` statements, including inline imports"), and note that `sos.services.bus.discovery` is the only exception allowed (shared infra pending kernel move, see pyproject R1 ignore).
Outcome: test file created; `pytest tests/contracts/test_mcp_no_service_imports.py` PASSES.

### Step 10 — CHANGELOG entry
File: `CHANGELOG.md`
Change: add `## [0.4.7] - 2026-04-18` block summarizing Phases 1–5 (squad/saas/billing/integrations via HTTP + AST sweep).
Outcome: CHANGELOG updated; `git diff CHANGELOG.md` shows only the new section.

### Step 11 — Tag + push
File: repo
Change: commit CHANGELOG + test with `chore(release): v0.4.7 — MCP R2 sweep complete`. `git tag v0.4.7`, push branch + tag.
Outcome: `git tag -l v0.4.7` exists; remote has the tag.

### Step 12 — v0.5.0 prep recon (task #78)
File: `sos/kernel/governance.py`, `sos/services/economy/metabolism.py`
Change: grep all `from sos.services.economy.metabolism` or similar imports inside `sos/kernel/`; document the leak shape in a v0.5 planning stub.
Outcome: written recon note in `docs/plans/2026-04-18-v0.5-kernel-metabolism.md` with carve approach options (move to kernel vs. HTTP client vs. event emission). No code changes — v0.5 is a separate sprint.

## Out of scope
- OpenAPI YAML for billing + integrations (deferred — nice-to-have, not blocking v0.4.7 tag).
- v0.5.0 code changes (only the recon doc lands this sprint).
- The remaining R1 ignores (`sos.services.*.** -> sos.services.bus.**`) — those wait for kernel/bus move.

## Budget
Main-context opus: ~$8. Sonnet specialists: 1 AST sweep + 2 OpenAPI (if we do them) ≈ $0.35. Total ~$8.35, well inside the 5-hour window.
