# SOS core dependencies
# For full install with all providers: pip install ".[full]"
# This file covers the minimum needed to run SOS services.

fastapi>=0.100.0
uvicorn>=0.23.0
httpx>=0.24.0
pydantic>=2.0.0
PyNaCl>=1.5.0
redis>=5.0.0
python-dotenv>=1.0.0
